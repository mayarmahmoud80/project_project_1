<section class="hero" style="padding:40px 0; text-align:center;">
  <div class="container">
    <h1>{{ 'welcome_message' | t }}</h1>
    <p class="lead">{{ 'hero_text' | t }}</p>
    <a class="btn" href="/collections">{{ 'shop_now' | t }}</a>
  </div>
</section>

{% schema %}
{
  "name": "Hero Banner",
  "settings": [
    { "type": "text", "id": "title", "label": "العنوان", "default": "أفضل المنتجات هنا" },
    { "type": "text", "id": "subtitle", "label": "النص الفرعي", "default": "تسوّق الآن واحصل على عروض" }
  ],
  "presets": [{ "name": "Hero Banner" }]
}
{% endschema %}
